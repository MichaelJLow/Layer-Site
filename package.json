{
  "name": "layer-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "tsc --noEmit",
    "check:links": "node scripts/check-published-links.mjs",
    "test": "npm run check:links",
    "ci": "npm run build && npm run typecheck && npm run check:links"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.0",
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.1.4",
    "@vercel/analytics": "^2.0.1",
    "astro": "^5.7.10",
    "tailwindcss": "^4.1.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
