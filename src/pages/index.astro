---
import Layout from '../components/Layout.astro';
import ContactForm from '../components/ContactForm.astro';
import { organizationJsonLd, site, websiteJsonLd } from '../config/site';

const pillars = [
  {
    slug: 'connect',
    title: 'Connect',
    impact: 'Bring the moving parts together.',
    description:
      'Connect the tools, data and information your business already depends on so work can move without people constantly copying, searching and re-entering information.',
    items: ['Tool & API integrations', 'Shared business context', 'Structured information flows'],
  },
  {
    slug: 'coordinate',
    title: 'Coordinate',
    impact: 'Move work forward without the chasing.',
    description: 'Route requests, trigger actions, prepare work and use AI where judgement genuinely helps.',
    items: ['Workflow orchestration', 'AI-assisted decisions', 'Routing, follow-ups and exceptions'],
  },
  {
    slug: 'control',
    title: 'Control',
    impact: 'Keep people in charge where it matters.',
    description: 'Put approvals, review points, audit trails and clear exception handling around consequential actions.',
    items: ['Human approval', 'Audit trails & visibility', 'Exceptions & manual override'],
  },
];

const steps = [
  {
    number: '01',
    tone: 'understand',
    title: '01 — Understand',
    description:
      'Start with a no-cost structured session to understand the business priority, surface likely opportunities and decide whether deeper investigation is justified.',
    service: 'AI Opportunity Session',
    output:
      'A paid AI Workflow Assessment maps one priority workflow, its value case, risks and decision-ready next step.',
    outputLabel: 'When justified',
    details: {
      lead:
        'A short, structured starting point that keeps the conversation focused on the business priority rather than the technology.',
      fit:
        'You know an important part of the business could work better, but you do not yet know whether AI, automation or a simpler process change is the right answer.',
      includes: [
        'A focused conversation about the priority, workflow and current friction',
        'Likely opportunity areas and obvious blockers',
        'A concise written summary with an honest recommendation on the next step',
      ],
      progression:
        'If deeper evidence is needed, a paid AI Workflow Assessment maps one priority workflow, builds the Value Case and measurement plan, and produces a decision-ready recommendation.',
      boundary:
        'The no-cost session is not solution design or implementation. The assessment may recommend implementation, a non-AI improvement, further investigation or no action.',
    },
  },
  {
    number: '02',
    tone: 'build',
    title: '02 — Build',
    description:
      'Start by redesigning and deploying one important workflow using the right mix of process change, automation, AI, integrations and human judgement. Once it is working and measured, we can extend into further workflows where the evidence supports it.',
    service: 'AI Workflow Implementation',
    output: 'A controlled first system and a proven foundation for further implementations.',
    outputLabel: 'Outcome',
    details: {
      lead:
        'A bounded implementation project that improves one important workflow and puts a controlled working system into real use.',
      fit:
        'The assessment has identified a workflow worth improving, and the intended value, scope, ownership, permissions and acceptance criteria are clear enough to build responsibly.',
      includes: [
        'Workflow redesign around the way the work actually needs to operate',
        'The minimum appropriate mix of process change, automation, AI and human judgement',
        'Controls, testing, staged rollout and visible failure handling',
        'Training, documentation, stabilisation, handover and outcome review',
      ],
      progression:
        'Layer starts with one clearly bounded workflow. Once it is operating and producing evidence, further workflows can be prioritised and implemented where they create credible value.',
      boundary:
        'Implementation does not imply unlimited support, silent autonomy or guaranteed returns. Ongoing monitoring, support and additional builds are scoped separately where appropriate.',
    },
  },
  {
    number: '03',
    tone: 'extend',
    title: '03 — Extend',
    description:
      'Where fragmented or untrusted knowledge is holding a workflow back, establish a trusted, current and permissioned foundation. When evidence supports it, expand into the next priority workflow.',
    service: 'AI Knowledge Foundation',
    output: 'Reliable knowledge and further improvements that earn their place through measured value.',
    outputLabel: 'Outcome',
    details: {
      lead:
        'An assessment-led path for workflows where useful AI is being held back by fragmented, outdated, unowned or unsafe operational knowledge.',
      fit:
        'People struggle to find the current answer, important knowledge lives across disconnected sources, or an AI-enabled workflow cannot safely rely on the information available today.',
      includes: [
        'A trusted knowledge foundation for one workflow or operational domain',
        'Clear ownership, permissions, sources and update responsibilities',
        'Traceable information that remains connected to authoritative business systems',
        'Client approval over what the organisation treats as company truth',
      ],
      progression:
        'Once the knowledge foundation is dependable, it can support the priority workflow and create a safer base for further implementations.',
      boundary:
        'This is not generic document migration, unrestricted enterprise search or a chatbot that silently decides what is true.',
    },
  },
  {
    number: '04',
    tone: 'improve',
    title: '04 — Improve',
    description:
      'An ongoing relationship for selected clients who want help prioritising the next opportunities, governing adoption and turning measured results into a coherent AI roadmap.',
    service: 'AI Enablement Partner',
    output:
      'Leadership stays focused on the highest-value next move, with progress, risk and value reviewed over time.',
    outputLabel: 'Outcome',
    details: {
      lead:
        'A selective ongoing relationship that gives leadership practical support for deciding where AI should go next and making sure adoption remains controlled and valuable.',
      fit:
        'You have completed an assessment, have confirmed fit with Layer and want an ongoing partner to help turn individual improvements into a coherent, evidence-led programme.',
      includes: [
        'A monthly leadership strategy and prioritisation session',
        'A monthly working session with the people moving the roadmap forward',
        'Stewardship of the AI opportunity roadmap',
        'Regular review of value, risk, controls and adoption',
        'Bounded research, recommendations and decision support',
      ],
      progression:
        'The relationship is reviewed regularly so that each next initiative earns its place through the Value Case, operating need and evidence from work already completed.',
      boundary:
        'Substantial implementation, production operations, incident response, major training and ongoing system monitoring are scoped separately.',
    },
  },
];

const systems = [
  {
    tone: 'ember',
    icon: 'inbox',
    title: 'Shared Inbox Routing & Approval',
    description:
      'Requests arriving by email are classified, matched with the right context and routed according to urgency and your business rules. Routine work keeps moving, while exceptions and sensitive actions are held for the right person to approve.',
    image: '/images/projects/opsdesk/opsdesk-dashboard-homepage.png',
    imageAlt: 'OpsDesk request queue with chargeable work awaiting approval',
    href: '/insights/shared-inbox-routing-and-approval',
    cta: 'See how it works',
  },
  {
    tone: 'mist',
    icon: 'sales',
    title: 'Sales opportunity intelligence',
    description:
      'New prospects are continuously researched, enriched and assessed against your ideal customer profile. The best opportunities flow into the CRM with useful context and tailored outreach ready for review.',
    image: '/images/projects/prospect-intelligence-system.png',
    imageAlt: 'Prospect intelligence workspace showing researched prospects, ICP fit and outreach ready for review',
    imageFit: 'contain',
    href: '/insights/sales-opportunity-intelligence',
    cta: 'See how it works',
  },
  {
    tone: 'rose',
    icon: 'marketing',
    title: 'Marketing content engine',
    description:
      'Calls, meetings and internal knowledge are continuously turned into content ideas and first drafts. Relevant material is organised, shaped for the right channels and moved towards publication, with your team reviewing what goes live.',
    image: '/images/projects/marketing-content-system.png',
    imageAlt: 'Marketing content workspace showing source material, draft content and publishing review',
    imageFit: 'contain',
    href: '/insights/marketing-content-engine',
    cta: 'See how it works',
  },
  {
    tone: 'violet',
    icon: 'invoice',
    title: 'Invoice processing automation',
    description:
      'Invoices that arrive by email are extracted, checked against your rules and prepared as a draft bill in Xero or the accounting software the business already uses. Payment stays held until finance approves them.',
    image: '/images/systems/invoiceflow-ap/control-centre.png',
    imageAlt:
      'InvoiceFlow AP invoice review workspace showing validation checks, approval routing, and a draft accounting preview.',
    imageFit: 'contain',
    href: '/insights/invoice-processing-automation',
    cta: 'See how it works',
  },
];

const valueMeasures = [
  {
    number: '01',
    title: 'Hard savings',
    description: 'Costs that genuinely disappear from the business—not just time saved on paper.',
  },
  {
    number: '02',
    title: 'Capacity released',
    description: 'Time and attention freed up for more valuable work.',
  },
  {
    number: '03',
    title: 'Cost avoidance',
    description: 'Future hiring, outsourcing, rework, penalties or tools the business may no longer need.',
  },
  {
    number: '04',
    title: 'Revenue and cash impact',
    description: 'Better conversion, throughput, retention, invoicing or collections.',
  },
  {
    number: '05',
    title: 'Risk and control improvement',
    description: 'Fewer errors, clearer accountability, stronger compliance and greater resilience.',
  },
  {
    number: '06',
    title: 'Service and experience improvement',
    description: 'Faster, more consistent and less frustrating experiences for customers and employees.',
  },
];

const faqs = [
  {
    question: 'What kinds of businesses do you work with?',
    answer: [
      'Layer works best with growing businesses where important work is spread across email, spreadsheets, documents and different software tools.',
      'The strongest fit is usually a business where repetitive admin, manual handoffs or disconnected information are starting to slow the team down.',
    ],
  },
  {
    question: 'Do we need to replace our existing software?',
    answer: [
      'Usually, no.',
      'Layer is designed to work around the tools your team already relies on. We connect the right systems, improve how information moves between them and only recommend replacing software where there is a clear reason to.',
    ],
  },
  {
    question: 'What do you automate, and what stays with our team?',
    answer: [
      'We automate the repetitive and predictable parts of a workflow first: moving information, checking rules, preparing work, routing requests and keeping systems updated.',
      'Where judgement, approval or accountability matters, the right person stays in control. The aim is not maximum automation. It is the right level of automation for the process.',
    ],
  },
  {
    question: 'How does a project begin?',
    answer: [
      'We normally start with a no-cost AI Opportunity Session focused on an important business priority.',
      'We surface the likely opportunity areas and obvious blockers, then recommend whether a paid AI Workflow Assessment is justified. If it is not, we’ll say so.',
    ],
  },
  {
    question: 'Can we start with just one workflow?',
    answer: [
      'Yes. That is usually the best way to start.',
      'We build one useful system around a clearly defined problem, prove it in practice and extend into other workflows only where it creates value. You do not need to commit to a large transformation upfront.',
    ],
  },
  {
    question: 'How do you handle security, access and sensitive actions?',
    answer: [
      'We design access around the minimum the system actually needs and keep important actions behind clear controls.',
      'Depending on the workflow, that can include restricted permissions, human approval, audit trails, exception handling and manual override. Security and control are considered as part of the system design, not added afterwards.',
    ],
  },
  {
    question: 'How long does a project take?',
    answer: [
      'It depends on the workflow, the systems involved and how much needs to be integrated.',
      'The Opportunity Session is usually about 45 minutes. If a paid assessment is justified, that produces the evidence needed to give you a realistic view of scope, complexity and timing before you commit to an implementation.',
    ],
  },
];

const serviceJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  name: site.brand,
  description: site.description,
  url: `${site.url}/`,
  email: site.email,
  image: new URL(site.logo, `${site.url}/`).href,
  serviceType: 'AI and automation systems for growing businesses',
  areaServed: 'Worldwide',
  founder: {
    '@type': 'Person',
    name: site.founder,
    url: `${site.url}/about/`,
    sameAs: [site.linkedin],
  },
};

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.answer.join(' '),
    },
  })),
  url: `${site.url}/#faq`,
};
---

<Layout title={site.title} description={site.description} jsonLd={[websiteJsonLd, organizationJsonLd, serviceJsonLd, faqJsonLd]}>
  <div class="layer-home">
    <section class="layer-hero" aria-labelledby="hero-title">
      <div class="layer-hero__atmosphere" aria-hidden="true"></div>
      <div class="layer-shell layer-hero__inner">
        <div class="layer-hero__copy" data-reveal data-hero-reveal>
          <h1 id="hero-title">
            <span class="layer-hero__line">
              <span class="layer-wordmark-inline" aria-label="Layer.">layer<span aria-hidden="true">.</span></span>
              {' '}builds the systems
            </span>
            <span class="layer-hero__line">that connect your team</span>
            <span class="layer-hero__line">and your tools.</span>
          </h1>
          <p class="layer-hero__intro">
            We help growing businesses identify where AI can create real value, then design and build the systems,
            workflows and automations that put it to work.
          </p>
          <div class="layer-actions">
            <a class="layer-button layer-button--light" href="#contact">
              Book a no-cost AI Opportunity Session <span aria-hidden="true">↗</span>
            </a>
            <a class="layer-link" href="#systems">Explore what Layer builds <span aria-hidden="true">↓</span></a>
          </div>
          <p class="layer-hero__note">No-cost AI Opportunity Session · no obligation</p>
        </div>
      </div>
      <div class="layer-shell layer-hero__legend" aria-hidden="true">
        <span>Team</span><i></i><strong>Layer</strong><i></i><span>Tools</span>
      </div>
    </section>

    <section id="what-we-build" class="layer-section layer-builds" aria-labelledby="builds-title">
      <div class="layer-shell">
        <header class="layer-section-head layer-section-head--model" data-reveal>
          <p class="layer-kicker">The operating model</p>
          <h2 id="builds-title">
            Connect the tools.<br />
            Coordinate the work.<br />
            Keep people in control.
          </h2>
          <p>Layer adds the operating layer between your team and the tools you already use.</p>
        </header>
        <div class="layer-triptych" data-reveal-stagger>
          {pillars.map((pillar) => (
            <article class={`layer-pillar layer-pillar--${pillar.slug}`} data-reveal>
              <div class="layer-pillar__copy">
                <h3>{pillar.title}</h3>
                <p class="layer-pillar__impact">{pillar.impact}</p>
                <p>{pillar.description}</p>
              </div>
              <div class="layer-pillar__examples">
                <p>Examples</p>
                <ul>
                  {pillar.items.map((item) => <li>{item}</li>)}
                </ul>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section id="how-we-work" class="layer-section layer-process" aria-labelledby="process-title">
      <div class="layer-shell">
        <header class="layer-section-head layer-process__head" data-reveal>
          <div>
            <p class="layer-kicker">From friction to flow</p>
            <h2 id="process-title">
              Start with one workflow.<br />
              Build from there.
            </h2>
          </div>
          <p>
            Start with the business value and one important workflow. Each next step must earn its place: assessment may
            lead to implementation, a knowledge foundation, a wider programme—or no build at all.
          </p>
        </header>
        <ol class="layer-step-grid" data-reveal-stagger>
          {steps.map((step) => (
            <li class={`layer-step--named layer-step--${step.tone}`} data-reveal>
              <div class="layer-step__atmosphere" aria-hidden="true">
                <div class="layer-step__glow"></div>
              </div>
              <p class="layer-step__phase">{step.title}</p>
              <div class="layer-step__mark" aria-hidden="true">
                {step.tone === 'understand' && (
                  <svg viewBox="0 0 48 48" fill="none">
                    <path d="M13 8.5h15.5L35 15v24.5H13V8.5Z" stroke="currentColor" stroke-width="1.4" />
                    <path d="M28.5 8.5V15H35" stroke="currentColor" stroke-width="1.4" />
                    <circle cx="23" cy="27" r="6.25" stroke="currentColor" stroke-width="1.4" />
                    <path d="M27.6 31.6 33.2 37.2" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" />
                  </svg>
                )}
                {step.tone === 'build' && (
                  <svg viewBox="0 0 48 48" fill="none">
                    <rect x="8" y="22" width="16" height="16" rx="2.2" stroke="currentColor" stroke-width="1.4" />
                    <rect x="16" y="10" width="16" height="16" rx="2.2" stroke="currentColor" stroke-width="1.4" />
                    <rect x="24" y="22" width="16" height="16" rx="2.2" stroke="currentColor" stroke-width="1.4" />
                  </svg>
                )}
                {step.tone === 'extend' && (
                  <svg viewBox="0 0 48 48" fill="none">
                    <circle cx="12.5" cy="14" r="4.25" stroke="currentColor" stroke-width="1.4" />
                    <circle cx="35.5" cy="14" r="4.25" stroke="currentColor" stroke-width="1.4" />
                    <circle cx="24" cy="34.5" r="4.25" stroke="currentColor" stroke-width="1.4" />
                    <path
                      d="M16.4 15.6 21.2 31.2M31.6 15.6 26.8 31.2M16.8 14h14.4"
                      stroke="currentColor"
                      stroke-width="1.4"
                    />
                  </svg>
                )}
                {step.tone === 'improve' && (
                  <svg viewBox="0 0 48 48" fill="none">
                    <path
                      d="M34.5 18.2a11.5 11.5 0 1 0 1.8 9.4"
                      stroke="currentColor"
                      stroke-width="1.4"
                      stroke-linecap="round"
                    />
                    <path d="M34.5 12.5v8.2h-8.2" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" />
                    <circle cx="24" cy="24.5" r="3.1" stroke="currentColor" stroke-width="1.4" />
                  </svg>
                )}
              </div>
              <h3>{step.service}</h3>
              <p>{step.description}</p>
              <button
                class="layer-step__more"
                type="button"
                data-service-dialog-open={`service-dialog-${step.tone}`}
                aria-haspopup="dialog"
                aria-controls={`service-dialog-${step.tone}`}
              >
                More about this service <span aria-hidden="true">→</span>
              </button>
              <div class="layer-step__output">
                <span>{step.outputLabel || 'Result'}</span>
                <p>{step.output}</p>
              </div>
            </li>
          ))}
        </ol>
        <aside class="layer-review" data-reveal>
          <div>
            <p class="layer-kicker">Your first step</p>
            <h3>Start with a no-cost AI Opportunity Session</h3>
          </div>
          <div>
            <p>
              We look at your business priority, surface likely opportunity areas and decide whether a paid assessment is
              justified. You receive a concise written summary and an honest recommendation on the right next step.
            </p>
            <a class="layer-button layer-button--primary" href="#contact">
              Book a no-cost AI Opportunity Session <span aria-hidden="true">↗</span>
            </a>
          </div>
        </aside>
        {steps.map((step) => (
          <dialog
            id={`service-dialog-${step.tone}`}
            class="layer-service-dialog"
            data-service-dialog
            aria-labelledby={`service-dialog-title-${step.tone}`}
          >
            <div class="layer-service-dialog__panel">
              <header class="layer-service-dialog__header">
                <p>{step.title}</p>
                <form method="dialog">
                  <button type="submit" aria-label={`Close ${step.service} details`}>
                    <span aria-hidden="true">×</span>
                  </button>
                </form>
              </header>
              <div class="layer-service-dialog__body">
                <p class="layer-kicker">Service details</p>
                <h2 id={`service-dialog-title-${step.tone}`}>{step.service}</h2>
                <p class="layer-service-dialog__lead">{step.details.lead}</p>

                <section>
                  <h3>When it fits</h3>
                  <p>{step.details.fit}</p>
                </section>

                <section>
                  <h3>What you get</h3>
                  <ul>
                    {step.details.includes.map((item) => <li>{item}</li>)}
                  </ul>
                </section>

                <section>
                  <h3>How it progresses</h3>
                  <p>{step.details.progression}</p>
                </section>

                <aside class="layer-service-dialog__boundary">
                  <span>Clear boundary</span>
                  <p>{step.details.boundary}</p>
                </aside>

                <a class="layer-service-dialog__cta" href="#contact" data-dialog-contact>
                  Start a conversation <span aria-hidden="true">→</span>
                </a>
              </div>
            </div>
          </dialog>
        ))}
      </div>
    </section>

    <section id="systems" class="layer-section layer-systems" aria-labelledby="systems-title">
      <div class="layer-systems__scroller" data-systems-scroll>
        <div class="layer-systems__sticky">
          <div class="layer-shell">
            <header class="layer-section-head layer-systems__head" data-reveal>
              <div>
                <p class="layer-kicker">Example systems</p>
                <h2 id="systems-title">Systems Layer can build</h2>
              </div>
              <p>Every system is shaped around the teams, tools and software already inside the business.</p>
            </header>
            <div class="layer-system-viewport">
              <div class="layer-system-track" data-systems-track>
          {systems.map((system) => (
            <article class={`layer-system-card layer-system-card--${system.tone}`}>
              <div class="layer-system-card__copy">
                <span class="layer-system-card__icon" aria-hidden="true">
                  {system.icon === 'inbox' && (
                    <svg viewBox="0 0 32 32" fill="none">
                      <path d="M16 6.8v11.2" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" />
                      <path d="M11.8 14.2 16 18.2 20.2 14.2" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" stroke-linejoin="round" />
                      <path d="M6.5 15.2h4.4l2 3.6h6.2l2-3.6h4.4V24.2H6.5V15.2Z" stroke="currentColor" stroke-width="1.45" stroke-linejoin="round" />
                    </svg>
                  )}
                  {system.icon === 'sales' && (
                    <svg viewBox="0 0 32 32" fill="none">
                      <circle cx="16" cy="16" r="8.4" stroke="currentColor" stroke-width="1.45" />
                      <circle cx="16" cy="16" r="3.6" stroke="currentColor" stroke-width="1.45" />
                      <path d="M16 5.6v2.8M16 23.6v2.8M5.6 16h2.8M23.6 16h2.8" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" />
                    </svg>
                  )}
                  {system.icon === 'marketing' && (
                    <svg viewBox="0 0 32 32" fill="none">
                      <rect x="8.5" y="6.8" width="15" height="18.4" rx="1.8" stroke="currentColor" stroke-width="1.45" />
                      <path d="M12.2 12.2h7.6M12.2 16h7.6M12.2 19.8h5" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" />
                    </svg>
                  )}
                  {system.icon === 'invoice' && (
                    <svg viewBox="0 0 32 32" fill="none">
                      <path d="M9.2 6.8h10.2L22.8 10.2V25.2H9.2V6.8Z" stroke="currentColor" stroke-width="1.45" stroke-linejoin="round" />
                      <path d="M19.4 6.8v3.6h3.4" stroke="currentColor" stroke-width="1.45" stroke-linejoin="round" />
                      <path d="M12.4 14.6h7.2M12.4 18.2h7.2M12.4 21.8h4.2" stroke="currentColor" stroke-width="1.45" stroke-linecap="round" />
                    </svg>
                  )}
                </span>
                <h3>{system.title}</h3>
                <p>{system.description}</p>
                {system.href ? (
                  <a class="layer-system-card__link" href={system.href}>
                    {system.cta} <span aria-hidden="true">→</span>
                  </a>
                ) : (
                  <span class="layer-system-card__soon">Case study coming soon</span>
                )}
              </div>
              <div class="layer-system-card__stage">
                <div class="layer-system-card__wash" aria-hidden="true"></div>
                {system.image ? (
                  <div class="layer-system-card__media">
                    <figure
                      class:list={[
                        'layer-system-card__shot',
                        system.imageFit === 'contain' && 'layer-system-card__shot--contain',
                      ]}
                    >
                      {system.href ? (
                        <a href={system.href} aria-label={`${system.cta}: ${system.title}`}>
                          <img
                            src={system.image}
                            alt={system.imageAlt || ''}
                            width="1024"
                            height="792"
                            loading="lazy"
                            decoding="async"
                          />
                        </a>
                      ) : (
                        <img
                          src={system.image}
                          alt={system.imageAlt || ''}
                          width="1024"
                          height="792"
                          loading="lazy"
                          decoding="async"
                        />
                      )}
                    </figure>
                  </div>
                ) : (
                  <div class="layer-system-card__placeholder">
                    <span>Screenshot coming soon</span>
                  </div>
                )}
              </div>
            </article>
          ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="value" class="layer-section layer-value" aria-labelledby="value-title">
      <div class="layer-shell">
        <header class="layer-section-head layer-value__head" data-reveal>
          <div>
            <p class="layer-kicker">How Layer measures value</p>
            <h2 id="value-title">Six ways better work can create value.</h2>
          </div>
          <p>
            We measure the change that matters to the business—not AI activity for its own sake. The right measure
            depends on the workflow and the outcome you are trying to improve.
          </p>
        </header>
        <ol class="layer-value__grid" data-reveal-stagger>
          {valueMeasures.map((measure) => (
            <li data-reveal>
              <span class="layer-value__number">{measure.number}</span>
              <div>
                <h3>{measure.title}</h3>
                <p>{measure.description}</p>
              </div>
            </li>
          ))}
        </ol>
        <aside class="layer-value__evidence" data-reveal>
          <p class="layer-kicker">Evidence before claims</p>
          <p>
            Every value case separates what is known, what is estimated and what is still unknown. Released capacity
            matters, but we only call it a cash saving when spend genuinely disappears.
          </p>
        </aside>
      </div>
    </section>

    <section id="faq" class="layer-section layer-faq" aria-labelledby="faq-title">
      <div class="layer-shell layer-faq__layout">
        <header class="layer-section-head" data-reveal>
          <p class="layer-kicker">Questions, answered</p>
          <h2 id="faq-title">Frequently asked questions</h2>
          <p>Clear answers about how Layer works, what we build and how a project begins.</p>
        </header>
        <div class="layer-faq__list" data-faq data-reveal>
          {faqs.map((item, index) => (
            <details open={index === 0}>
              <summary>
                <span>{item.question}</span>
                <i aria-hidden="true"></i>
              </summary>
              <div class="layer-faq__answer">
                {item.answer.map((paragraph) => <p>{paragraph}</p>)}
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <section id="contact" class="layer-contact" aria-labelledby="contact-title">
      <div class="layer-contact__atmosphere" aria-hidden="true"></div>
      <div class="layer-shell layer-contact__layout">
        <div class="layer-contact__copy" data-reveal>
          <p class="layer-kicker">Start a conversation</p>
          <h2 id="contact-title">Where is work getting stuck in your business?</h2>
          <p>
            Tell us about the business priority or workflow that isn’t working as well as it should. We’ll surface the
            likely opportunity areas and decide whether deeper assessment is justified.
          </p>
          <p class="layer-contact__note">No-cost AI Opportunity Session. No obligation.</p>
        </div>
        <div data-reveal>
          <ContactForm variant="home" />
        </div>
      </div>
    </section>
  </div>

  <script>
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const revealItems = Array.from(document.querySelectorAll<HTMLElement>('[data-reveal]'));

    if (reducedMotion || !('IntersectionObserver' in window)) {
      revealItems.forEach((item) => item.classList.add('is-visible'));
    } else {
      document.querySelectorAll<HTMLElement>('[data-hero-reveal]').forEach((item) => item.classList.add('is-visible'));
      const revealObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (!entry.isIntersecting) return;
            entry.target.classList.add('is-visible');
            revealObserver.unobserve(entry.target);
          });
        },
        { threshold: 0.12, rootMargin: '0px 0px -7% 0px' },
      );
      revealItems.forEach((item) => {
        if (!item.hasAttribute('data-hero-reveal')) revealObserver.observe(item);
      });
    }

    document.querySelectorAll<HTMLDetailsElement>('[data-faq] details').forEach((detail) => {
      detail.addEventListener('toggle', () => {
        if (!detail.open) return;
        document.querySelectorAll<HTMLDetailsElement>('[data-faq] details').forEach((other) => {
          if (other !== detail) other.open = false;
        });
      });
    });

    document.querySelectorAll<HTMLButtonElement>('[data-service-dialog-open]').forEach((button) => {
      const dialogId = button.dataset.serviceDialogOpen;
      const dialog = dialogId ? document.getElementById(dialogId) as HTMLDialogElement | null : null;
      if (!dialog) return;

      button.addEventListener('click', () => {
        dialog.showModal();
        dialog.querySelector<HTMLButtonElement>('.layer-service-dialog__header button')?.focus();
      });
      dialog.addEventListener('close', () => button.focus());
    });

    document.querySelectorAll<HTMLDialogElement>('[data-service-dialog]').forEach((dialog) => {
      dialog.addEventListener('click', (event) => {
        const bounds = dialog.getBoundingClientRect();
        const outside =
          event.clientX < bounds.left ||
          event.clientX > bounds.right ||
          event.clientY < bounds.top ||
          event.clientY > bounds.bottom;
        if (outside) dialog.close();
      });

      dialog.querySelectorAll<HTMLElement>('[data-dialog-contact]').forEach((link) => {
        link.addEventListener('click', () => dialog.close());
      });
    });

    document.addEventListener('keydown', (event) => {
      if (event.key !== 'Escape') return;
      const openDialog = document.querySelector<HTMLDialogElement>('[data-service-dialog][open]');
      if (!openDialog) return;
      event.preventDefault();
      openDialog.close();
    });

    const systemsScroller = document.querySelector<HTMLElement>('[data-systems-scroll]');
    const systemsTrack = document.querySelector<HTMLElement>('[data-systems-track]');
    const systemsSticky = systemsScroller?.querySelector<HTMLElement>('.layer-systems__sticky');
    const desktopSystems = window.matchMedia('(min-width: 821px)');

    if (systemsScroller && systemsTrack && systemsSticky) {
      const viewport = systemsTrack.parentElement as HTMLElement;
      let ticking = false;

      const canPin = () => !reducedMotion && desktopSystems.matches;

      const updateSystems = () => {
        ticking = false;
        if (!canPin()) return;
        const pin = parseFloat(getComputedStyle(systemsSticky).top) || 0;
        const rect = systemsScroller.getBoundingClientRect();
        const range = Math.max(1, systemsScroller.offsetHeight - window.innerHeight);
        const progress = Math.min(1, Math.max(0, (pin - rect.top) / range));
        const maxX = Math.max(0, systemsTrack.scrollWidth - viewport.clientWidth);
        systemsTrack.style.transform = `translate3d(${-maxX * progress}px, 0, 0)`;
      };

      const layoutSystems = () => {
        viewport.style.setProperty('--systems-w', `${viewport.clientWidth}px`);
        if (!canPin()) {
          systemsScroller.style.height = '';
          systemsTrack.style.transform = '';
          return;
        }
        const maxX = Math.max(0, systemsTrack.scrollWidth - viewport.clientWidth);
        systemsScroller.style.height = `${window.innerHeight + maxX * 1.15}px`;
        updateSystems();
      };

      const onSystemsScroll = () => {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(updateSystems);
      };

      layoutSystems();
      requestAnimationFrame(layoutSystems);
      window.addEventListener('scroll', onSystemsScroll, { passive: true });
      window.addEventListener('resize', layoutSystems);
      window.addEventListener('load', layoutSystems);
      desktopSystems.addEventListener('change', layoutSystems);
      if ('ResizeObserver' in window) {
        new ResizeObserver(layoutSystems).observe(viewport);
      }
    }
  </script>
</Layout>

<style>
  .layer-home {
    --layer-paper: #f6f4ef;
    --layer-paper-soft: #eceae5;
    --layer-ink: #111317;
    --layer-muted: #555d69;
    --layer-navy: #1d3557;
    --layer-navy-deep: #10233e;
    --layer-line: rgba(17, 19, 23, 0.13);
    color: var(--layer-ink);
    background: var(--layer-paper);
    font-family: 'IBM Plex Sans', system-ui, sans-serif;
  }

  .layer-home *,
  .layer-home *::before,
  .layer-home *::after {
    box-sizing: border-box;
  }

  .layer-home a {
    text-decoration: none;
  }

  .layer-shell {
    width: min(88rem, calc(100% - 4rem));
    margin-inline: auto;
  }

  .layer-section {
    padding-block: clamp(6rem, 10vw, 10rem);
    scroll-margin-top: 4.75rem;
  }

  .layer-kicker {
    margin: 0;
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.66rem;
    font-weight: 500;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .layer-section-head {
    max-width: 47rem;
  }

  .layer-section-head h2 {
    margin: 0.9rem 0 0;
    font-size: clamp(2.7rem, 5.6vw, 5.8rem);
    font-weight: 400;
    letter-spacing: -0.065em;
    line-height: 0.98;
    text-wrap: balance;
  }

  .layer-section-head > p:last-child {
    max-width: 38rem;
    margin: 1.4rem 0 0;
    color: var(--layer-muted);
    font-size: clamp(1rem, 1.4vw, 1.15rem);
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-button {
    display: inline-flex;
    min-height: 3.15rem;
    align-items: center;
    justify-content: center;
    gap: 1.35rem;
    padding: 0.85rem 1.2rem;
    border: 1px solid transparent;
    border-radius: 0.2rem;
    font-size: 0.86rem;
    font-weight: 500;
    cursor: pointer;
    transition: transform 220ms ease, background 220ms ease, box-shadow 220ms ease;
  }

  .layer-button:hover {
    transform: translateY(-2px);
  }

  .layer-button--primary {
    color: #fff;
    background: var(--layer-navy);
    box-shadow: 0 12px 30px rgba(29, 53, 87, 0.2);
  }

  .layer-button--primary:hover {
    color: #fff;
    background: var(--layer-navy-deep);
    box-shadow: 0 16px 34px rgba(29, 53, 87, 0.26);
  }

  .layer-button--light {
    color: var(--layer-navy-deep);
    background: #f8f6f1;
  }

  .layer-hero {
    --hero-period: #6eb4e8;
    position: relative;
    isolation: isolate;
    min-height: 100svh;
    margin-top: -4.75rem;
    overflow: hidden;
    display: flex;
    align-items: center;
    color: #f4f5f7;
    background: #101b2b;
  }

  .layer-hero__atmosphere {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    background:
      radial-gradient(ellipse 70% 60% at 0% 0%, rgba(72, 178, 186, 0.58), transparent 62%),
      radial-gradient(ellipse 74% 66% at 100% 100%, rgba(224, 122, 48, 0.7), transparent 60%),
      #101b2b;
  }

  .layer-hero__atmosphere::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(ellipse 52% 46% at 6% 4%, rgba(90, 196, 198, 0.36), transparent 70%),
      radial-gradient(ellipse 56% 50% at 94% 96%, rgba(240, 148, 68, 0.4), transparent 68%);
    filter: blur(48px);
  }

  .layer-hero__atmosphere::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.5;
    mix-blend-mode: overlay;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.9'/%3E%3C/svg%3E");
  }

  .layer-shell.layer-hero__inner {
    position: relative;
    z-index: 2;
    width: min(92rem, calc(100% - 3rem));
    padding-block: clamp(6.5rem, 12vh, 9rem) clamp(6.5rem, 11vh, 9rem);
    text-align: center;
  }

  .layer-hero__copy {
    width: 100%;
    margin-inline: auto;
  }

  .layer-hero h1 {
    margin: 0 auto;
    color: #fff;
    font-size: clamp(1.45rem, 4.6vw, 4.85rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 1.08;
  }

  .layer-hero__line {
    display: block;
    white-space: nowrap;
  }

  .layer-hero h1 .layer-wordmark-inline {
    color: inherit;
    font-weight: 450;
    letter-spacing: -0.065em;
    white-space: nowrap;
  }

  .layer-hero h1 .layer-wordmark-inline span {
    color: var(--hero-period);
  }

  .layer-hero__intro {
    max-width: 38rem;
    margin: 2rem auto 0;
    color: rgba(244, 245, 247, 0.72);
    font-size: clamp(1.0625rem, 1.55vw, 1.28rem);
    font-weight: 300;
    line-height: 1.6;
  }

  .layer-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem 1.4rem;
    margin-top: 1.45rem;
  }

  .layer-hero .layer-actions {
    justify-content: center;
  }

  .layer-link {
    display: inline-flex;
    gap: 0.65rem;
    align-items: center;
    color: var(--layer-ink);
    border-bottom: 1px solid rgba(17, 19, 23, 0.35);
    padding: 0.75rem 0;
    font-size: 0.86rem;
  }

  .layer-link:hover {
    color: var(--layer-navy);
    border-color: var(--layer-navy);
  }

  .layer-hero .layer-button--light {
    color: #10233e;
    background: #f8f6f1;
    box-shadow: none;
  }

  .layer-hero .layer-button--light:hover {
    background: #fff;
  }

  .layer-hero .layer-link {
    color: rgba(244, 245, 247, 0.88);
    border-bottom-color: rgba(244, 245, 247, 0.38);
  }

  .layer-hero .layer-link:hover {
    color: #fff;
    border-bottom-color: #fff;
  }

  .layer-hero__note {
    margin: 0.9rem auto 0;
    color: rgba(244, 245, 247, 0.45);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .layer-hero__legend {
    position: absolute;
    z-index: 3;
    bottom: 1.3rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: rgba(244, 245, 247, 0.42);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.56rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .layer-hero__legend i {
    width: 1.4rem;
    height: 1px;
    background: rgba(244, 245, 247, 0.28);
  }

  .layer-hero__legend strong {
    color: #fff;
    font-weight: 500;
  }

  .layer-builds {
    position: relative;
    overflow: hidden;
    color: var(--layer-ink);
    background: #efede8;
  }

  .layer-contact::after,
  .layer-systems__sticky::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.08;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    mix-blend-mode: soft-light;
  }

  .layer-systems .layer-shell,
  .layer-contact .layer-shell {
    position: relative;
    z-index: 1;
  }

  .layer-section-head--light .layer-kicker {
    color: #c7d4e4;
  }

  .layer-section-head--light > p:last-child {
    color: rgba(244, 245, 247, 0.68);
  }

  .layer-section-head--model {
    max-width: 52rem;
  }

  .layer-section-head--model .layer-kicker {
    color: var(--layer-navy);
  }

  .layer-section-head--model h2 {
    color: var(--layer-ink);
    font-size: clamp(2.15rem, 4.4vw, 4.1rem);
    line-height: 1.06;
    text-wrap: pretty;
  }

  .layer-section-head--model > p:last-child {
    color: var(--layer-muted);
  }

  .layer-triptych {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
    margin-top: clamp(2.5rem, 5vw, 4rem);
  }

  .layer-pillar {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    min-height: 0;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 1.75rem;
    padding: clamp(1.25rem, 2.2vw, 1.75rem);
    background: #27384a;
    box-shadow: 0 18px 48px rgba(0, 0, 0, 0.18);
    transition: transform 300ms ease;
  }

  .layer-pillar::before {
    content: '';
    position: absolute;
    inset: -20%;
    z-index: -1;
    filter: blur(34px);
    transform: scale(1.05);
    transition: transform 700ms ease;
  }

  .layer-pillar:hover {
    transform: translateY(-4px);
  }

  .layer-pillar:hover::before {
    transform: scale(1.12) translate3d(1%, -1%, 0);
  }

  .layer-pillar--connect::before {
    background:
      radial-gradient(circle at 24% 18%, rgba(230, 160, 122, 0.94), transparent 36%),
      radial-gradient(circle at 83% 65%, rgba(97, 143, 149, 0.8), transparent 45%),
      #263b52;
  }

  .layer-pillar--coordinate::before {
    background:
      radial-gradient(circle at 72% 25%, rgba(223, 171, 140, 0.85), transparent 38%),
      radial-gradient(circle at 20% 82%, rgba(75, 91, 137, 0.86), transparent 42%),
      #564452;
  }

  .layer-pillar--control::before {
    background:
      radial-gradient(circle at 38% 28%, rgba(152, 187, 180, 0.88), transparent 40%),
      radial-gradient(circle at 76% 84%, rgba(197, 110, 92, 0.7), transparent 42%),
      #24384a;
  }

  .layer-pillar__copy {
    margin: 0;
    padding: 0;
  }

  .layer-pillar h3 {
    margin: 0;
    color: #fff;
    font-size: clamp(2rem, 3.2vw, 2.85rem);
    font-weight: 500;
    letter-spacing: -0.055em;
  }

  .layer-pillar__impact {
    margin: 0.85rem 0 0;
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.95rem;
    font-weight: 500;
    line-height: 1.45;
  }

  .layer-pillar__copy > p:last-child {
    margin: 0.7rem 0 0;
    color: rgba(255, 255, 255, 0.68);
    font-size: 0.9rem;
    font-weight: 300;
    line-height: 1.6;
  }

  .layer-pillar__examples {
    margin: 0;
    padding: 1rem 0 0;
    border-top: 1px solid rgba(255, 255, 255, 0.18);
  }

  .layer-pillar__examples > p {
    margin: 0 0 0.55rem;
    color: rgba(255, 255, 255, 0.42);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .layer-pillar ul {
    display: grid;
    gap: 0.5rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .layer-pillar li {
    color: rgba(255, 255, 255, 0.62);
    font-size: 0.78rem;
    line-height: 1.45;
  }

  .layer-process {
    background: #efede8;
  }

  .layer-process__head,
  .layer-systems__head {
    max-width: none;
    display: grid;
    grid-template-columns: 1fr minmax(20rem, 0.55fr);
    gap: clamp(3rem, 9vw, 9rem);
    align-items: end;
  }

  .layer-process__head > p,
  .layer-systems__head > p {
    margin: 0;
    color: var(--layer-muted);
    font-size: 1.05rem;
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-step-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1rem;
    margin: clamp(3rem, 6vw, 5.5rem) 0 0;
    padding: 0;
    list-style: none;
  }

  .layer-step-grid li {
    position: relative;
    isolation: isolate;
    overflow: visible;
    display: flex;
    flex-direction: column;
    min-height: 28rem;
    padding: 1.5rem;
    border: 1px solid rgba(29, 53, 87, 0.13);
    border-radius: 0.55rem;
    background: rgba(250, 249, 246, 0.72);
    box-shadow: 0 18px 48px rgba(40, 48, 61, 0.07);
  }

  .layer-step__atmosphere {
    position: absolute;
    inset: 0;
    z-index: -1;
    overflow: hidden;
    border-radius: inherit;
    pointer-events: none;
  }

  .layer-step__glow {
    position: absolute;
    z-index: -1;
    inset: -30%;
    opacity: 0.42;
    filter: blur(25px);
    transition: transform 800ms ease, opacity 500ms ease;
  }

  .layer-step--understand .layer-step__glow {
    background:
      radial-gradient(circle at 22% 18%, rgba(110, 160, 196, 0.22), transparent 32%),
      radial-gradient(circle at 78% 82%, rgba(70, 112, 128, 0.12), transparent 36%);
  }

  .layer-step--build .layer-step__glow {
    background:
      radial-gradient(circle at 24% 16%, rgba(218, 135, 106, 0.24), transparent 32%),
      radial-gradient(circle at 80% 84%, rgba(163, 96, 77, 0.1), transparent 36%);
  }

  .layer-step--extend .layer-step__glow {
    background:
      radial-gradient(circle at 20% 20%, rgba(107, 152, 155, 0.22), transparent 32%),
      radial-gradient(circle at 76% 80%, rgba(70, 112, 128, 0.12), transparent 36%);
  }

  .layer-step--improve .layer-step__glow {
    background:
      radial-gradient(circle at 26% 18%, rgba(137, 122, 168, 0.2), transparent 32%),
      radial-gradient(circle at 78% 82%, rgba(160, 99, 99, 0.1), transparent 36%);
  }

  .layer-step-grid li:hover .layer-step__glow {
    opacity: 0.7;
    transform: translate3d(4%, -3%, 0) scale(1.08);
  }

  @media (min-width: 1081px) {
    .layer-step-grid li:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 50%;
      right: -0.5rem;
      z-index: 2;
      width: 0.34rem;
      height: 0.34rem;
      border-block-start: 1px solid rgba(29, 53, 87, 0.28);
      border-inline-end: 1px solid rgba(29, 53, 87, 0.28);
      transform: translate(50%, -50%) rotate(45deg);
      pointer-events: none;
    }
  }

  .layer-step__mark {
    position: relative;
    display: grid;
    place-items: center;
    width: 2.75rem;
    height: 2.75rem;
    margin: 1.15rem 0 0.15rem;
    color: var(--layer-navy);
  }

  .layer-step__mark::before {
    content: '';
    position: absolute;
    inset: -0.55rem;
    z-index: -1;
    border-radius: 999px;
    filter: blur(10px);
    opacity: 0.5;
  }

  .layer-step--understand .layer-step__mark::before {
    background: rgba(110, 160, 196, 0.2);
  }

  .layer-step--build .layer-step__mark::before {
    background: rgba(218, 135, 106, 0.22);
  }

  .layer-step--extend .layer-step__mark::before {
    background: rgba(107, 152, 155, 0.22);
  }

  .layer-step--improve .layer-step__mark::before {
    background: rgba(137, 122, 168, 0.2);
  }

  .layer-step__mark svg {
    width: 2.05rem;
    height: 2.05rem;
    display: block;
  }

  .layer-step__top {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .layer-step__top > span:first-child {
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.65rem;
  }

  .layer-step__phase {
    margin: clamp(4rem, 7vw, 6.5rem) 0 0;
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .layer-step-grid h3 {
    margin: 0.65rem 0 0;
    font-size: clamp(1.45rem, 2vw, 2rem);
    font-weight: 500;
    letter-spacing: -0.04em;
    line-height: 1.1;
  }

  .layer-step-grid p {
    margin: 1rem 0 0;
    color: var(--layer-muted);
    font-size: 0.9rem;
    font-weight: 300;
    line-height: 1.65;
  }

  .layer-step-grid .layer-step__phase {
    margin-top: clamp(4rem, 7vw, 6.5rem);
  }

  .layer-step-grid li.layer-step--named .layer-step__phase {
    margin-top: 0;
  }

  .layer-step--understand .layer-step__phase::after,
  .layer-step--build .layer-step__phase::after,
  .layer-step--extend .layer-step__phase::after {
    content: ' →';
    font-weight: 400;
    letter-spacing: 0;
    color: rgba(29, 53, 87, 0.38);
  }

  .layer-step__more {
    display: inline-flex;
    align-items: center;
    align-self: flex-start;
    gap: 0.65rem;
    margin: 1.15rem 0 1.35rem;
    padding: 0;
    border: 0;
    color: #ad5f3f;
    background: transparent;
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.66rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    line-height: 1.4;
    text-align: left;
    cursor: pointer;
    transition: color 180ms ease, transform 180ms ease;
  }

  .layer-step__more:hover {
    color: #7f3f28;
    transform: translateX(0.15rem);
  }

  .layer-step__more:focus-visible {
    border-radius: 0.12rem;
    outline: 2px solid var(--layer-navy);
    outline-offset: 0.3rem;
  }

  .layer-step__output {
    margin-top: auto;
    min-height: 8.5rem;
    padding-top: 1.15rem;
    border-top: 1px solid rgba(29, 53, 87, 0.14);
  }

  .layer-step__output > span {
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .layer-step-grid .layer-step__output p {
    margin-top: 0.45rem;
    color: #3f4854;
    font-size: 0.78rem;
    line-height: 1.5;
  }

  .layer-review {
    display: grid;
    grid-template-columns: 1fr minmax(22rem, 0.8fr);
    gap: clamp(3rem, 8vw, 8rem);
    align-items: start;
    margin-top: 1rem;
    padding: clamp(2rem, 4vw, 3.5rem);
    color: #f4f5f7;
    background:
      radial-gradient(circle at 4% 5%, rgba(191, 117, 90, 0.45), transparent 35%),
      linear-gradient(135deg, #172b47, #1d3557 58%, #354e62);
    box-shadow: 0 28px 70px rgba(29, 53, 87, 0.18);
  }

  .layer-review .layer-kicker {
    color: #e0a07a;
  }

  .layer-review h3 {
    max-width: 12ch;
    margin: 1rem 0 0;
    font-size: clamp(2.3rem, 4.6vw, 4.8rem);
    font-weight: 400;
    line-height: 0.98;
    letter-spacing: -0.06em;
  }

  .layer-review > div:last-child > p {
    margin: 0 0 1.7rem;
    color: rgba(255, 255, 255, 0.72);
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-review .layer-button--primary {
    color: var(--layer-navy-deep);
    background: #f5f3ee;
    box-shadow: none;
  }

  .layer-service-dialog {
    position: fixed;
    inset: 0 0 0 auto;
    width: min(40rem, 100%);
    max-width: 100%;
    height: 100dvh;
    max-height: 100dvh;
    margin: 0 0 0 auto;
    padding: 0;
    overflow: hidden;
    border: 0;
    color: var(--layer-ink);
    background: var(--layer-paper);
    box-shadow: -28px 0 80px rgba(16, 35, 62, 0.2);
  }

  .layer-service-dialog[open] {
    animation: layer-dialog-enter 280ms ease-out both;
  }

  .layer-service-dialog::backdrop {
    background: rgba(12, 22, 36, 0.48);
    backdrop-filter: blur(4px);
  }

  .layer-service-dialog__panel {
    height: 100%;
    overflow-y: auto;
    overscroll-behavior: contain;
  }

  .layer-service-dialog__header {
    position: sticky;
    top: 0;
    z-index: 2;
    display: flex;
    min-height: 4.75rem;
    align-items: center;
    justify-content: space-between;
    padding: 0 1.5rem;
    border-bottom: 1px solid var(--layer-line);
    background: rgba(246, 244, 239, 0.94);
    backdrop-filter: blur(14px);
  }

  .layer-service-dialog__header > p {
    margin: 0;
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.66rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .layer-service-dialog__header form {
    margin: 0;
  }

  .layer-service-dialog__header button {
    display: grid;
    width: 2.5rem;
    height: 2.5rem;
    place-items: center;
    padding: 0;
    border: 1px solid rgba(29, 53, 87, 0.16);
    border-radius: 999px;
    color: var(--layer-navy);
    background: transparent;
    font-size: 1.5rem;
    font-weight: 300;
    line-height: 1;
    cursor: pointer;
  }

  .layer-service-dialog__header button:hover {
    background: rgba(29, 53, 87, 0.06);
  }

  .layer-service-dialog__header button:focus-visible {
    outline: 2px solid var(--layer-navy);
    outline-offset: 0.2rem;
  }

  .layer-service-dialog__body {
    padding: clamp(2rem, 6vw, 4.5rem) clamp(1.5rem, 5vw, 4rem) 4rem;
  }

  .layer-service-dialog__body > h2 {
    max-width: 12ch;
    margin: 0.8rem 0 0;
    font-size: clamp(2.4rem, 5vw, 4.2rem);
    font-weight: 400;
    letter-spacing: -0.06em;
    line-height: 0.98;
  }

  .layer-service-dialog__lead {
    margin: 1.4rem 0 0;
    color: #3f4854;
    font-size: 1.1rem;
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-service-dialog__body > section {
    margin-top: 2.4rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--layer-line);
  }

  .layer-service-dialog__body > section h3 {
    margin: 0;
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.68rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .layer-service-dialog__body > section p,
  .layer-service-dialog__body > section li,
  .layer-service-dialog__boundary p {
    color: var(--layer-muted);
    font-size: 0.95rem;
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-service-dialog__body > section p {
    margin: 0.8rem 0 0;
  }

  .layer-service-dialog__body > section ul {
    display: grid;
    gap: 0.7rem;
    margin: 0.9rem 0 0;
    padding: 0;
    list-style: none;
  }

  .layer-service-dialog__body > section li {
    position: relative;
    padding-left: 1.15rem;
  }

  .layer-service-dialog__body > section li::before {
    content: '';
    position: absolute;
    top: 0.72em;
    left: 0;
    width: 0.3rem;
    height: 0.3rem;
    border-radius: 999px;
    background: #ad5f3f;
  }

  .layer-service-dialog__boundary {
    margin-top: 2.4rem;
    padding: 1.25rem;
    border-left: 2px solid rgba(173, 95, 63, 0.65);
    background: rgba(255, 255, 255, 0.45);
  }

  .layer-service-dialog__boundary > span {
    color: #91482e;
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.64rem;
    font-weight: 500;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .layer-service-dialog__boundary p {
    margin: 0.6rem 0 0;
  }

  .layer-service-dialog__cta {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    margin-top: 2rem;
    color: var(--layer-navy);
    font-size: 0.88rem;
    font-weight: 500;
  }

  .layer-service-dialog__cta:hover {
    color: #ad5f3f;
  }

  @keyframes layer-dialog-enter {
    from {
      opacity: 0;
      transform: translateX(2rem);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .layer-systems {
    position: relative;
    color: #f4f5f7;
    background: #101b2b;
  }

  .layer-systems.layer-section {
    padding-block: 0;
  }

  .layer-systems__scroller {
    position: relative;
    z-index: 1;
  }

  .layer-systems__sticky {
    position: sticky;
    top: 4.75rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: calc(100svh - 4.75rem);
    min-height: calc(100svh - 4.75rem);
    padding-block: 1.5rem 1.65rem;
    box-sizing: border-box;
    background:
      radial-gradient(ellipse 45% 60% at 6% 7%, rgba(163, 96, 77, 0.45), transparent 64%),
      radial-gradient(ellipse 55% 72% at 94% 100%, rgba(78, 116, 121, 0.4), transparent 64%),
      #101b2b;
  }

  .layer-systems__sticky .layer-shell {
    display: flex;
    flex-direction: column;
    flex: 0 1 auto;
    min-height: 0;
    width: min(88rem, calc(100% - 4rem));
  }

  .layer-systems__head {
    flex-shrink: 0;
  }

  .layer-systems .layer-kicker {
    color: #e0a07a;
  }

  .layer-systems .layer-systems__head > p {
    color: rgba(244, 245, 247, 0.68);
  }

  .layer-systems .layer-systems__head h2 {
    color: #fff;
  }

  .layer-system-viewport {
    container-type: inline-size;
    flex: none;
    min-height: 0;
    margin-top: 1.35rem;
    overflow: hidden;
  }

  .layer-system-track {
    display: flex;
    align-items: stretch;
    gap: 1rem;
    height: auto;
    min-height: 0;
    will-change: transform;
  }

  .layer-system-card {
    display: flex;
    flex-direction: column;
    flex: 0 0 calc((var(--systems-w, 100cqi) - 1rem) / 2);
    width: calc((var(--systems-w, 100cqi) - 1rem) / 2);
    min-width: 0;
    min-height: 0;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.4);
    border-radius: 1.35rem;
    background: rgba(248, 246, 241, 0.88);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.62),
      0 18px 40px rgba(8, 16, 28, 0.12);
    backdrop-filter: blur(14px) saturate(108%);
    transition: transform 300ms ease, border-color 300ms ease, box-shadow 300ms ease;
  }

  .layer-system-card:hover {
    border-color: rgba(255, 255, 255, 0.32);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.42),
      0 24px 48px rgba(8, 16, 28, 0.2);
  }

  .layer-system-card__copy {
    display: grid;
    grid-template-columns: auto 1fr;
    column-gap: 0.85rem;
    row-gap: 0.4rem;
    align-items: start;
    padding: 1.15rem 1.2rem 0.7rem;
    background: transparent;
  }

  .layer-system-card__icon {
    display: grid;
    place-items: center;
    width: 2.55rem;
    height: 2.55rem;
    border: 1px solid rgba(29, 53, 87, 0.12);
    border-radius: 0.55rem;
    color: var(--layer-navy);
    background: rgba(29, 53, 87, 0.05);
  }

  .layer-system-card__icon svg {
    width: 1.2rem;
    height: 1.2rem;
  }

  .layer-system-card h3,
  .layer-system-card__copy > p,
  .layer-system-card__link,
  .layer-system-card__soon {
    grid-column: 2;
  }

  .layer-system-card h3 {
    margin: 0.15rem 0 0;
    max-width: 28ch;
    color: var(--layer-ink);
    font-size: clamp(1.45rem, 2.1vw, 1.85rem);
    font-weight: 500;
    letter-spacing: -0.045em;
    line-height: 1.08;
  }

  .layer-system-card__copy > p {
    margin: 0;
    max-width: 36rem;
    color: #1a2433;
    font-size: 0.9rem;
    font-weight: 400;
    line-height: 1.6;
  }

  .layer-system-card__link {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    margin-top: 0.15rem;
    color: var(--layer-navy);
    font-size: 0.84rem;
    font-weight: 500;
  }

  .layer-system-card__link:hover {
    color: var(--layer-navy-deep);
  }

  .layer-system-card__soon {
    margin-top: 0.15rem;
    color: var(--layer-muted);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .layer-system-card__stage {
    position: relative;
    isolation: isolate;
    display: grid;
    place-items: start stretch;
    flex: none;
    min-height: 0;
    padding: 0 1.2rem 1.1rem;
    overflow: hidden;
  }

  .layer-system-card__wash {
    display: none;
  }

  .layer-system-card__media {
    position: relative;
    z-index: 1;
    width: 100%;
  }

  .layer-system-card__shot {
    position: relative;
    margin: 0;
    overflow: hidden;
    aspect-ratio: 16 / 10;
    border: 0;
    border-radius: 0.85rem;
    background: transparent;
    box-shadow: 0 18px 36px rgba(8, 16, 28, 0.22);
  }

  .layer-system-card__shot a {
    display: block;
    height: 100%;
    color: inherit;
    text-decoration: none;
  }

  .layer-system-card__shot img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: left top;
  }

  .layer-system-card__shot--contain {
    background: transparent;
  }

  .layer-system-card__shot--contain img {
    object-fit: contain;
    object-position: center;
  }

  .layer-system-card__placeholder {
    display: grid;
    place-items: center;
    min-height: 12rem;
    color: var(--layer-muted);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .layer-value {
    position: relative;
    overflow: hidden;
    background: #e7e3dc;
  }

  .layer-value::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(circle at 8% 16%, rgba(108, 159, 177, 0.12), transparent 26rem),
      radial-gradient(circle at 92% 84%, rgba(205, 132, 91, 0.13), transparent 28rem);
  }

  .layer-value .layer-shell {
    position: relative;
  }

  .layer-value__head {
    max-width: none;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(20rem, 0.55fr);
    gap: clamp(3rem, 9vw, 9rem);
    align-items: end;
  }

  .layer-value__head h2 {
    max-width: 12ch;
    font-size: clamp(2.7rem, 5vw, 5.2rem);
  }

  .layer-value__head > p {
    margin: 0;
  }

  .layer-value__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin: clamp(3rem, 6vw, 5rem) 0 0;
    padding: 0;
    border-top: 1px solid rgba(17, 19, 23, 0.18);
    border-left: 1px solid rgba(17, 19, 23, 0.18);
    list-style: none;
  }

  .layer-value__grid li {
    position: relative;
    min-height: 15rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 2.5rem;
    padding: clamp(1.35rem, 2.4vw, 2rem);
    border-right: 1px solid rgba(17, 19, 23, 0.18);
    border-bottom: 1px solid rgba(17, 19, 23, 0.18);
    background: rgba(250, 249, 246, 0.38);
  }

  .layer-value__grid li::before {
    content: '';
    position: absolute;
    inset: 0 auto auto 0;
    width: 100%;
    height: 2px;
    opacity: 0.72;
    background: var(--layer-navy);
    transform: scaleX(0.18);
    transform-origin: left;
    transition: transform 360ms ease;
  }

  .layer-value__grid li:nth-child(2n)::before {
    background: #c9794d;
  }

  .layer-value__grid li:hover::before {
    transform: scaleX(1);
  }

  .layer-value__number {
    color: var(--layer-navy);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.67rem;
    letter-spacing: 0.12em;
  }

  .layer-value__grid h3 {
    max-width: 17ch;
    margin: 0;
    font-size: clamp(1.45rem, 2.1vw, 2rem);
    font-weight: 500;
    letter-spacing: -0.045em;
    line-height: 1.08;
  }

  .layer-value__grid p {
    max-width: 32rem;
    margin: 0.8rem 0 0;
    color: var(--layer-muted);
    font-size: 0.92rem;
    font-weight: 300;
    line-height: 1.62;
  }

  .layer-value__evidence {
    display: grid;
    grid-template-columns: minmax(10rem, 0.36fr) minmax(0, 1fr);
    gap: 2rem;
    align-items: start;
    margin-top: 1rem;
    padding: 1.35rem 1.5rem;
    border: 1px solid rgba(29, 53, 87, 0.15);
    background: rgba(248, 246, 241, 0.64);
  }

  .layer-value__evidence > p:last-child {
    max-width: 54rem;
    margin: 0;
    color: var(--layer-muted);
    font-size: 0.9rem;
    font-weight: 300;
    line-height: 1.65;
  }

  .layer-faq {
    background: var(--layer-paper);
  }

  .layer-faq__layout {
    display: grid;
    grid-template-columns: minmax(18rem, 0.62fr) minmax(28rem, 1fr);
    gap: clamp(4rem, 10vw, 11rem);
    align-items: start;
  }

  .layer-faq__layout .layer-section-head {
    position: sticky;
    top: 8rem;
  }

  .layer-faq__layout .layer-section-head h2 {
    font-size: clamp(2.7rem, 4.8vw, 4.8rem);
  }

  .layer-faq__list {
    border-top: 1px solid var(--layer-line);
  }

  .layer-faq details {
    border-bottom: 1px solid var(--layer-line);
  }

  .layer-faq summary {
    min-height: 5.5rem;
    display: grid;
    grid-template-columns: 1fr 1.3rem;
    gap: 1.5rem;
    align-items: center;
    cursor: pointer;
    list-style: none;
    color: var(--layer-ink);
    font-size: clamp(1.05rem, 1.5vw, 1.28rem);
    font-weight: 500;
  }

  .layer-faq summary::-webkit-details-marker {
    display: none;
  }

  .layer-faq summary i {
    position: relative;
    width: 1.1rem;
    height: 1.1rem;
  }

  .layer-faq summary i::before,
  .layer-faq summary i::after {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    width: 100%;
    height: 1px;
    background: var(--layer-navy);
    transition: transform 220ms ease;
  }

  .layer-faq summary i::after {
    transform: rotate(90deg);
  }

  .layer-faq details[open] summary i::after {
    transform: rotate(0);
  }

  .layer-faq__answer {
    overflow: hidden;
    padding: 0 3rem 1.7rem 0;
  }

  .layer-faq__answer p {
    max-width: 42rem;
    margin: 0 0 0.75rem;
    color: var(--layer-muted);
    font-size: 0.96rem;
    font-weight: 300;
    line-height: 1.7;
  }

  .layer-contact {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    scroll-margin-top: 4.75rem;
    padding-block: clamp(6rem, 10vw, 10rem);
    color: #f5f6f8;
    background: #142742;
  }

  .layer-contact__atmosphere {
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      radial-gradient(ellipse 45% 65% at 4% 14%, rgba(194, 113, 86, 0.52), transparent 62%),
      radial-gradient(ellipse 50% 75% at 100% 100%, rgba(92, 137, 140, 0.48), transparent 64%),
      linear-gradient(130deg, #152943, #1d3557 55%, #263f52);
  }

  .layer-contact__layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(26rem, 0.75fr);
    gap: clamp(4rem, 10vw, 10rem);
    align-items: start;
  }

  .layer-contact .layer-kicker {
    color: #e0a07a;
  }

  .layer-contact h2 {
    max-width: 11ch;
    margin: 1rem 0 0;
    font-size: clamp(3rem, 6vw, 6.5rem);
    font-weight: 400;
    letter-spacing: -0.068em;
    line-height: 0.95;
  }

  .layer-contact__copy > p:not(.layer-kicker, .layer-contact__note) {
    max-width: 38rem;
    margin: 1.6rem 0 0;
    color: rgba(255, 255, 255, 0.72);
    font-size: 1.03rem;
    font-weight: 300;
    line-height: 1.72;
  }

  .layer-contact__note {
    margin: 2rem 0 0;
    color: rgba(255, 255, 255, 0.52);
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  [data-reveal] {
    opacity: 0;
    transform: translateY(18px);
    transition: opacity 650ms cubic-bezier(0.22, 1, 0.36, 1), transform 650ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  [data-reveal].is-visible {
    opacity: 1;
    transform: none;
  }

  [data-reveal-stagger] > [data-reveal]:nth-child(2) { transition-delay: 70ms; }
  [data-reveal-stagger] > [data-reveal]:nth-child(3) { transition-delay: 140ms; }
  [data-reveal-stagger] > [data-reveal]:nth-child(4) { transition-delay: 210ms; }
  [data-reveal-stagger] > [data-reveal]:nth-child(5) { transition-delay: 280ms; }

  @media (max-width: 1080px) {
    .layer-triptych,
    .layer-step-grid,
    .layer-value__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .layer-pillar:last-child {
      grid-column: span 2;
    }

    .layer-faq__layout {
      grid-template-columns: 1fr;
      gap: 3.5rem;
    }

    .layer-faq__layout .layer-section-head {
      position: static;
    }
  }

  @media (max-width: 820px) {
    .layer-shell {
      width: min(100% - 2rem, 46rem);
    }

    .layer-hero {
      min-height: 100svh;
      align-items: flex-start;
    }

    .layer-shell.layer-hero__inner {
      width: min(100% - 1.5rem, 92rem);
      padding-block: 6.5rem 11rem;
    }

    .layer-process__head,
    .layer-systems__head,
    .layer-value__head,
    .layer-review,
    .layer-contact__layout {
      grid-template-columns: 1fr;
    }

    .layer-process__head,
    .layer-systems__head,
    .layer-value__head {
      gap: 1.75rem;
    }

    .layer-triptych,
    .layer-step-grid,
    .layer-value__grid {
      grid-template-columns: 1fr;
    }

    .layer-value__evidence {
      grid-template-columns: 1fr;
      gap: 0.8rem;
    }

    .layer-pillar,
    .layer-pillar:last-child {
      grid-column: auto;
      min-height: 0;
    }

    .layer-step-grid li {
      min-height: 22rem;
    }

    .layer-review {
      gap: 2rem;
    }

    .layer-systems__scroller {
      height: auto !important;
    }

    .layer-systems__sticky {
      position: static;
      height: auto;
      min-height: 0;
      padding-block: clamp(6rem, 10vw, 10rem);
    }

    .layer-system-viewport {
      overflow-x: auto;
      overflow-y: hidden;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      overscroll-behavior-x: contain;
      scrollbar-width: none;
      touch-action: pan-x;
    }

    .layer-system-viewport::-webkit-scrollbar {
      display: none;
    }

    .layer-system-track {
      transform: none !important;
      will-change: auto;
      height: auto;
      min-height: 0;
    }

    .layer-system-card {
      flex: 0 0 min(22.5rem, calc(var(--systems-w, 85cqi) * 0.85));
      width: min(22.5rem, calc(var(--systems-w, 85cqi) * 0.85));
      scroll-snap-align: start;
    }

    .layer-contact__layout {
      gap: 3.5rem;
    }
  }

  @media (max-width: 560px) {
    .layer-section {
      padding-block: 5rem;
    }

    .layer-hero h1 {
      font-size: clamp(1.2rem, 6.4vw, 2.35rem);
    }

    .layer-actions {
      align-items: stretch;
      flex-direction: column;
    }

    .layer-button {
      width: 100%;
    }

    .layer-link {
      justify-content: center;
      border: 0;
    }

    .layer-hero__legend {
      justify-content: center;
    }

    .layer-pillar,
    .layer-step-grid li,
    .layer-system-card__copy,
    .layer-review {
      padding: 1.25rem;
    }

    .layer-step__output {
      min-height: 0;
    }

    .layer-service-dialog__header {
      padding-inline: 1.1rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    [data-reveal] {
      opacity: 1;
      transform: none;
      transition: none;
    }

    .layer-pillar,
    .layer-pillar::before,
    .layer-step__glow,
    .layer-system-card,
    .layer-system-card__wash {
      transition: none;
    }

    .layer-service-dialog[open] {
      animation: none;
    }

    .layer-systems__scroller {
      height: auto !important;
    }

    .layer-systems__sticky {
      position: static;
      height: auto;
      min-height: 0;
      padding-block: clamp(6rem, 10vw, 10rem);
    }

    .layer-system-viewport {
      overflow: visible;
    }

    .layer-system-track {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
      height: auto;
      min-height: 0;
      transform: none !important;
    }

    .layer-system-card {
      flex: none;
      width: auto;
    }
  }

  @media (prefers-reduced-motion: reduce) and (max-width: 820px) {
    .layer-system-track {
      grid-template-columns: 1fr;
    }
  }
</style>
